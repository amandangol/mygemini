import 'package:flutter/material.dart';

class ImageCaptioner extends StatelessWidget {
  const ImageCaptioner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
